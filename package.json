{
  "name": "wiredalter-status",
  "version": "2.0.0",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node --dns-result-order=ipv4first server.js",
    "dev": "node --dns-result-order=ipv4first --watch server.js",
    "build:css": "npx @tailwindcss/cli -i ./views/input.css -o ./public/style.css --minify"
  },
  "dependencies": {
    "better-sqlite3": "^12.6.2",
    "cron": "^4.4.0",
    "csrf-sync": "^4.2.1",
    "ejs": "^4.0.1",
    "express": "^5.2.1",
    "express-rate-limit": "^8.2.1",
    "express-session": "^1.19.0",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.0",
    "tailwindcss": "^4.2.0"
  }
}
