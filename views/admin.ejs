<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .glass { background: rgba(30, 41, 59, 0.7); backdrop-filter: blur(10px); border: 1px solid rgba(255,255,255,0.1); }
        /* Custom scrollbar */
        ::-webkit-scrollbar { width: 6px; height: 6px; }
        ::-webkit-scrollbar-track { background: #0f172a; }
        ::-webkit-scrollbar-thumb { background: #334155; border-radius: 3px; }
        ::-webkit-scrollbar-thumb:hover { background: #475569; }
    </style>
</head>
<body class="bg-[#0b1120] text-slate-300 min-h-screen font-sans">

    <nav class="border-b border-slate-800/60 bg-[#0b1120]/50 backdrop-blur-md sticky top-0 z-50">
        <div class="max-w-6xl mx-auto px-6 h-16 flex justify-between items-center">
            <div class="flex items-center gap-3">
                <div class="w-2 h-2 rounded-full bg-blue-500"></div>
                <h1 class="font-bold text-white tracking-tight">Status Admin</h1>
            </div>
            <div class="flex gap-6 text-sm font-medium">
                <a href="/" target="_blank" class="text-slate-400 hover:text-white transition-colors">View Public Page ↗</a>
                <a href="/logout" class="text-red-400 hover:text-red-300 transition-colors">Logout</a>
            </div>
        </div>
    </nav>

    <main class="max-w-6xl mx-auto px-6 py-10 relative">
        
        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
            
            <div class="lg:col-span-1">
                <div class="glass rounded-xl p-6 sticky top-24">
                    <h2 class="text-lg font-bold text-white mb-6 flex items-center gap-2">
                        <svg class="w-5 h-5 text-emerald-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path></svg>
                        New Monitor
                    </h2>
                    
                    <form action="/api/monitors" method="POST" class="space-y-5">
                        <div>
                            <label class="block text-xs font-bold uppercase text-slate-500 mb-1.5">Friendly Name</label>
                            <input type="text" name="name" required placeholder="e.g. Production API" 
                                class="w-full bg-[#0f172a] border border-slate-700 rounded-lg px-3 py-2.5 text-sm text-white focus:outline-none focus:border-blue-500 transition-colors">
                        </div>

                        <div>
                            <label class="block text-xs font-bold uppercase text-slate-500 mb-1.5">Target URL</label>
                            <input type="url" name="url" required placeholder="https://api.example.com/health" 
                                class="w-full bg-[#0f172a] border border-slate-700 rounded-lg px-3 py-2.5 text-sm text-white focus:outline-none focus:border-blue-500 transition-colors">
                            <p class="text-[10px] text-slate-600 mt-1">Must return a 200-299 HTTP status code.</p>
                        </div>

                        <div>
                            <label class="block text-xs font-bold uppercase text-slate-500 mb-1.5">Notification Webhook (Optional)</label>
                            <input type="url" name="notification_url" placeholder="https://ntfy.sh/..." 
                                class="w-full bg-[#0f172a] border border-slate-700 rounded-lg px-3 py-2.5 text-sm text-white focus:outline-none focus:border-blue-500 transition-colors">
                            <p class="text-[10px] text-slate-600 mt-1">Supports Discord, Ntfy, Gotify.</p>
                        </div>

                        <div>
                            <label class="block text-xs font-bold uppercase text-slate-500 mb-1.5">Auth Token (Optional)</label>
                            <input type="password" name="notification_token" placeholder="tk_..." 
                                class="w-full bg-[#0f172a] border border-slate-700 rounded-lg px-3 py-2.5 text-sm text-white focus:outline-none focus:border-blue-500 transition-colors">
                            <p class="text-[10px] text-slate-600 mt-1">Bearer token for private Ntfy topics.</p>
                        </div>

                        <div>
                            <label class="block text-xs font-bold uppercase text-slate-500 mb-1.5">Check Interval (Seconds)</label>
                            <input type="number" name="interval" value="60" min="10" required 
                                class="w-full bg-[#0f172a] border border-slate-700 rounded-lg px-3 py-2.5 text-sm text-white focus:outline-none focus:border-blue-500 transition-colors">
                        </div>

                        <button type="submit" class="w-full bg-emerald-600 hover:bg-emerald-500 text-white font-semibold py-2.5 rounded-lg transition-colors mt-2 text-sm shadow-lg shadow-emerald-900/20">
                            Create Monitor
                        </button>
                    </form>
                </div>
            </div>

            <div class="lg:col-span-2 space-y-6">
                <div class="flex justify-between items-end mb-2">
                    <h2 class="text-lg font-bold text-white">Active Monitors</h2>
                    <span class="text-xs text-slate-500"><%= monitors.length %> configured</span>
                </div>

                <% if (monitors.length === 0) { %>
                    <div class="glass rounded-xl p-12 text-center border-dashed border-2 border-slate-800">
                        <div class="inline-flex p-4 rounded-full bg-slate-800/50 mb-4 text-slate-600">
                            <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path></svg>
                        </div>
                        <h3 class="text-white font-medium">No monitors yet</h3>
                        <p class="text-slate-500 text-sm mt-1">Use the form on the left to add your first service.</p>
                    </div>
                <% } else { %>
                    <div class="space-y-3">
                        <% monitors.forEach(m => { %>
                            <div class="glass rounded-lg p-4 flex flex-col sm:flex-row sm:items-center justify-between gap-4 group hover:bg-slate-800/30 transition-colors">
                                <div class="flex items-center gap-4">
                                    <div class="w-10 h-10 rounded-lg bg-[#0f172a] border border-slate-700 flex items-center justify-center shrink-0">
                                        <% if (m.status === 'up') { %>
                                            <div class="w-2.5 h-2.5 rounded-full bg-emerald-500 shadow-[0_0_8px_rgba(16,185,129,0.4)]"></div>
                                        <% } else if (m.status === 'down') { %>
                                            <div class="w-2.5 h-2.5 rounded-full bg-red-500 shadow-[0_0_8px_rgba(239,68,68,0.4)]"></div>
                                        <% } else { %>
                                            <div class="w-2.5 h-2.5 rounded-full bg-yellow-500 animate-pulse"></div>
                                        <% } %>
                                    </div>
                                    <div>
                                        <h3 class="font-semibold text-white"><%= m.name %></h3>
                                        <div class="flex items-center gap-2 text-xs text-slate-500 font-mono mt-0.5">
                                            <span class="truncate max-w-[150px]"><%= m.url %></span>
                                            <span class="text-slate-700">•</span>
                                            <span><%= m.interval %>s</span>
                                        </div>
                                    </div>
                                </div>

                                <div class="flex items-center gap-2 sm:justify-end border-t sm:border-0 border-slate-800 pt-3 sm:pt-0">
                                    
                                    <button onclick="openEditModal(<%= JSON.stringify(m) %>)" class="p-2 text-slate-500 hover:text-blue-400 hover:bg-blue-500/10 rounded-lg transition-all" title="Edit Monitor">
                                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path></svg>
                                    </button>

                                    <form action="/api/monitors/delete/<%= m.id %>" method="POST" onsubmit="return confirm('Delete this monitor?');">
                                        <button type="submit" class="p-2 text-slate-500 hover:text-red-400 hover:bg-red-500/10 rounded-lg transition-all" title="Delete Monitor">
                                            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path></svg>
                                        </button>
                                    </form>
                                </div>
                            </div>
                        <% }) %>
                    </div>
                <% } %>
            </div>
        </div>
    </main>

    <div id="editModal" class="fixed inset-0 z-[100] hidden" aria-labelledby="modal-title" role="dialog" aria-modal="true">
        <div class="fixed inset-0 bg-slate-900/80 backdrop-blur-sm transition-opacity" onclick="closeEditModal()"></div>
        <div class="fixed inset-0 z-10 overflow-y-auto">
            <div class="flex min-h-full items-center justify-center p-4 text-center">
                <div class="relative transform overflow-hidden rounded-2xl bg-[#0f172a] border border-slate-700 text-left shadow-2xl transition-all w-full max-w-lg">
                    <form id="editForm" method="POST" class="p-6 space-y-5">
                        <h3 class="text-lg font-bold text-white mb-4">Edit Monitor</h3>
                        
                        <div>
                            <label class="block text-xs font-bold uppercase text-slate-500 mb-1.5">Friendly Name</label>
                            <input type="text" id="edit_name" name="name" required class="w-full bg-[#0b1120] border border-slate-700 rounded-lg px-3 py-2.5 text-sm text-white focus:border-blue-500">
                        </div>
                        <div>
                            <label class="block text-xs font-bold uppercase text-slate-500 mb-1.5">Target URL</label>
                            <input type="url" id="edit_url" name="url" required class="w-full bg-[#0b1120] border border-slate-700 rounded-lg px-3 py-2.5 text-sm text-white focus:border-blue-500">
                        </div>
                        <div>
                            <label class="block text-xs font-bold uppercase text-slate-500 mb-1.5">Notification Webhook</label>
                            <input type="url" id="edit_notify_url" name="notification_url" class="w-full bg-[#0b1120] border border-slate-700 rounded-lg px-3 py-2.5 text-sm text-white focus:border-blue-500">
                        </div>
                        <div>
                            <label class="block text-xs font-bold uppercase text-slate-500 mb-1.5">Auth Token</label>
                            <input type="password" id="edit_notify_token" name="notification_token" class="w-full bg-[#0b1120] border border-slate-700 rounded-lg px-3 py-2.5 text-sm text-white focus:border-blue-500" placeholder="(Unchanged)">
                        </div>
                        <div>
                            <label class="block text-xs font-bold uppercase text-slate-500 mb-1.5">Check Interval (Seconds)</label>
                            <input type="number" id="edit_interval" name="interval" min="10" required class="w-full bg-[#0b1120] border border-slate-700 rounded-lg px-3 py-2.5 text-sm text-white focus:border-blue-500">
                        </div>

                        <div class="flex justify-end gap-3 mt-6">
                            <button type="button" onclick="closeEditModal()" class="px-4 py-2 text-sm font-medium text-slate-400 hover:text-white transition-colors">Cancel</button>
                            <button type="submit" class="px-4 py-2 text-sm font-bold text-white bg-blue-600 rounded-lg hover:bg-blue-500 shadow-lg shadow-blue-900/20">Save Changes</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <script>
        function openEditModal(monitor) {
            document.getElementById('edit_name').value = monitor.name;
            document.getElementById('edit_url').value = monitor.url;
            document.getElementById('edit_interval').value = monitor.interval;
            document.getElementById('edit_notify_url').value = monitor.notification_url || '';
            document.getElementById('edit_notify_token').value = monitor.notification_token || '';
            
            // Set the form action dynamically
            document.getElementById('editForm').action = `/api/monitors/edit/${monitor.id}`;
            
            document.getElementById('editModal').classList.remove('hidden');
        }

        function closeEditModal() {
            document.getElementById('editModal').classList.add('hidden');
        }
    </script>

</body>
</html>
